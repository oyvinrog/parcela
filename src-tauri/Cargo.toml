[package]
name = "parcela-tauri"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
parcela = { path = ".." }
rfd = "0.14"
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }
